- 多选文件后右键“属性”现在打开合并属性窗口，与资源管理器一致显示所选项目的文件/文件夹数量和总大小；回收站多选同样适用。
- 修复 Alt+Enter 快捷键无法打开属性窗口的问题（菜单中已标注该快捷键）。
- 多选时底部状态栏显示所选文件的总大小；内容搜索结果在后台汇总完成后显示。选中大小按选择状态缓存，大量结果全选时不会拖慢界面绘制。
- 修复框选后依赖选择状态的缓存可能不刷新的问题。
- 调试状态栏的帧率改为统计最近约 1 秒内连续绘制的实际帧数，不再把空闲间隔显示为极低帧率，或把相邻两次重绘显示为异常高的帧率。

继续提供 Windows 10/11 x64 和 Windows 8.1 x64 两种安装包。
