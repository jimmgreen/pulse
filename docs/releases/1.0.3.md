- 新增自动检查更新：启动后后台检查，有新版时提示，点击后下载、校验并启动安装。
- 普通版与 Windows 8.1 兼容版分别获取对应安装包。
- 索引迁移支持选择磁盘根目录，自动使用其中的 `Index` 文件夹。
- 索引服务配置统一保存到 `%PROGRAMDATA%\Pulse\index-config.json`，兼容原有配置。
- 修复索引迁移后空分卷目录和旧索引目录残留的问题。
