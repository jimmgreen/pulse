- 修复搜索结果中的多选会被意外清除的问题：此前只要电脑上有任何文件变化，或者滚动到结果列表底部加载更多，已选中的多个结果就会只剩一项。现在会按完整路径保留全部已选项和焦点项。
- 修复没有选中任何搜索结果时，后台刷新会自动选中第一行的问题。
- 搜索页面空闲时大幅降低 CPU 占用：索引服务只在搜索结果真正变化时才推送新结果，不再因为无关文件变化反复发送整页相同的结果。此前打开搜索页面时，界面进程在空闲状态下可能持续占用 25% 以上的 CPU。

继续提供 Windows 10/11 x64 和 Windows 8.1 x64 两种安装包。
