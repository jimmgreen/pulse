- 修复内容搜索结果在实时更新时选中项会"跑到"别的文件上的问题：此前在内容搜索结果页选中一个文件后，只要有其他文件开始或不再匹配搜索词（例如被新建、修改或删除），列表重新排序后高亮和后续操作（删除、重命名、打开）就会落到另一个文件上。现在会按完整路径重新定位，始终保持在原来选中的文件上。
- 修复没有选中任何内容搜索结果时，实时更新会自动选中第一行的问题。
- 侧边栏"标签"入口改用彩色 Fluent 图标，与收藏、下载、回收站等图标风格统一。

继续提供 Windows 10/11 x64 和 Windows 8.1 x64 两种安装包。
