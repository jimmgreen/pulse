# 索引重构方案:启动快 · 搜索快 · 占用低

> 2026-08-16。阶段 2.5 索引上线后的回归:启动冻结数秒~数十秒、稳态内存 ≈220MB。
> 本文结论:**不做补丁式优化,重构索引的存储与并发模型**。现有设计里有几处
> 本身就是"为了优化而优化"的产物,恰恰是它们造成了卡顿和内存问题。

---

## 0. 目标(硬指标)

| 目标 | 指标 | 现状 |
| --- | --- | --- |
| 启动快 | 窗口出现 → 可交互 **≤ 200ms**;启动后 30s 内无大规模磁盘扫描 | 冻结数秒~数十秒 + 每次启动全盘重扫 |
| 搜索快 | 首字符出结果 ≤ 150ms;增量收窄 ≤ 50ms;全盘查询(300 万条)≤ 100ms | 满足(但重建期间会被锁卡住) |
| 占用低 | 任务管理器私有工作集:**空闲 ≤ 60MB**;搜索后 ≤ 100MB | ≈220MB 常驻 |

---

## 1. 现状测量

| 项目 | 实测值 | 说明 |
| --- | --- | --- |
| `%LOCALAPPDATA%\Pulse\pulse-index.bin` | **166.7 MB** | 缓存 v5,每条 ≈31B 头 + 名字 UTF-16 |
| 估算索引条数 | **≈300 万** | 166MB ÷ (31B + 平均 13 字符 × 2B) |
| 稳态工作集 | **≈220 MB** | 与下面分解吻合 |
| 索引模式 | **walk 模式**(非管理员) | 无 USN 检查点 → 每次启动全量重扫 |

### 220MB 的构成(300 万节点,全部常驻堆内存)

| 结构 | 合计 | 判定 |
| --- | --- | --- |
| `nodes`(parent+off+len+flags,12B) | 36 MB | 必要数据,但**不必在堆上** |
| `pool`(名字 UTF-16,≈26B) | 78 MB | 同上 |
| `frns`(8B) | 24 MB | walk 模式全为 0,**纯浪费** |
| `sizes` + `mtimes`(16B) | 48 MB | 必要数据,可压缩、可离堆 |
| `by_name_`/`by_size_`/`by_mtime_`(12B) | 36 MB | **过度设计,应删除**(见 2.1) |
| 合计 | **≈222 MB** | 与实测一致;UI 侧占用可忽略 |

---

## 2. 方向检讨:哪些是"为了优化而优化"

### 2.1 全局排序索引(by_name_/by_size_/by_mtime_)——删除

为"搜索结果按列排序"预建了 3 份全量排序索引(36MB),启动时在**锁内**
对 300 万条做 3 次全量排序,比较器每次还构造 2 个 `std::wstring` 调
`StrCmpLogicalW`(`index_engine.cpp:438-444`)——这就是启动冻结的直接原因,
且每 5 分钟在锁内重建一次(`Worker:1188`),使用中周期性复发。

但它服务的场景根本不需要它:搜索页最多物化 10 万行,**对结果集按需排序**
一次只要几十 ms。为了省这几十 ms,付出了 36MB 常驻 + 启动数十秒 + 周期性
冻结。**方向错了,整个子系统删除。**

### 2.2 "把整个索引解析进堆内存"——改为文件即索引(mmap)

现状:启动时把 166MB 缓存逐条 `f.read`(300 万条 × 6 次小读)解析成堆上
5 个 vector;退出/定时再逐条写回。启动成本、220MB 常驻、保存时 +166MB
快照拷贝,全部源于"磁盘格式 ≠ 内存格式"。

正确方向是 Everything 同款思路的进一步版:**磁盘格式 = 内存格式**。
索引文件按节可直接 `MapViewOfFile` 使用,"加载"变成映射(<10ms、零拷贝、
零解析)。文件页由 OS 按需换入换出——**file-backed 页不计入任务管理器
显示的"私有工作集"**,内存压力大时 OS 自动回收,无需自己管理。

### 2.3 walk 模式默认索引全部磁盘——收窄默认范围

没有 USN 就没有廉价的失效手段,"全盘索引"在非管理员下是一个兑现不了的
承诺:只能每次启动重扫全盘(几分钟磁盘+CPU),索引还是会陈旧。
**默认只索引用户目录 + 用户自选文件夹**(RDCW 可以真正盯住的范围),
全盘搜索作为显式能力:管理员 MFT 模式,或用户手动开启全盘 walk。
条数从 300 万降到 ~40 万,内存/文件/重扫时间全线 ÷10。

### 2.4 一把大锁贯穿读写——读写分离

UI 每帧 `BuildVm` → `index.Status()` 都要抢引擎大锁(`app_main.cpp:446`),
而 worker 持锁做排序、USN 回放 DeviceIoControl(`index_engine.cpp:1119-1133`)、
`WalkTree` 每插一条拿一次锁(300 万次,`index_engine.cpp:1024-1030`)。
搜索(命令面板/搜索页)也在 UI 线程同步调 `Search()`。
任何一个写路径变慢,UI 直接冻结。**并发模型必须重构**:读者永不阻塞。

### 2.5 运行期 O(N) 路径解析

`FindChildLocked` 找一个孩子要线性扫全部节点(`index_engine.cpp:188-195`),
RDCW 每个文件事件都要按路径逐层解析 → 单个事件锁内 100ms 级。编译一次
项目触发几百个事件,UI 掉帧。范围收窄(2.3)后 N 变小可缓解,但结构上
应补 `(parent, 折叠名 hash) → idx` 查找表,事件处理降到 µs 级。

---

## 3. 目标架构

```
┌─ pulse.exe ──────────────────────────────────────────────┐
│  UI 线程:只读原子快照(status/count),永不碰索引锁      │
│  查询线程:搜索在后台跑,带代际号,结果 PostMessage 回 UI │
│                                                           │
│  索引 = 基座 + 增量(LSM 两层)                           │
│  ┌─────────────────────────┐  ┌────────────────────────┐  │
│  │ 基座:pulse-index.bin    │  │ 增量(堆上,很小)      │  │
│  │ mmap 只读,节对齐:      │  │ 新增节点 append store  │  │
│  │  header/nodes/attrs/    │  │ 删除 = tombstone set   │  │
│  │  pool/frn 表            │  │ 改名 = 覆盖表          │  │
│  │ file-backed,不计私有WS │  │ 来源:RDCW / USN 回放  │  │
│  └─────────────────────────┘  └────────────────────────┘  │
│  后台合并:增量>10 万条或 24h → 重写 .tmp + rename + 重映射│
└──────────────────────────────────────────────────────────┘
```

### 3.1 索引文件 v6(即最终内存布局)

| 节 | 内容 | 300 万条大小 |
| --- | --- | --- |
| header | magic/版本/各节偏移/卷检查点(USN)/构建时间戳 | <1 KB |
| nodes | parent i32 + off u32 + len u16 + flags u8 + pad,16B 对齐 | 48 MB |
| attrs | mtime **u32**(Unix 秒)+ size u64 | 36 MB |
| pool | 名字 UTF-16 连续存放 | 78 MB |
| frn 表 | 每卷 (frn u64, idx i32) 有序数组,仅 MFT 模式非空 | 0(walk) |

- 文件 ≈162MB(全盘 300 万条)/ **≈22MB(默认用户域 40 万条)**。
- 写盘:内存组好整块 buffer 一次写 `.tmp`,`MoveFileExW` 原子替换。
  彻底消灭 300 万次小读写和保存时的全量快照拷贝。
- 搜索直接扫映射的 nodes+pool:首次冷查询付一次顺序换页
  (NVMe ≈2GB/s,120MB ≈60ms),之后全部命中页缓存。

### 3.2 并发模型:读写分离

- 基座只读不可变;增量用**单独小锁**保护(操作都是 µs 级:append/查 hash)。
- `Status()`/`Count()` 改为纯原子变量,UI 每帧读零成本。
- 搜索线程:快照 (基座指针, 增量代际) 后无锁扫描;重建/合并产生新基座后
  原子换指针,旧映射引用计数归零后 unmap。
- 命令面板与搜索页的 `Search()` 移出 UI 线程(带代际号防过期结果上屏)。

### 3.3 查询执行(保持现有能力,去掉全局排序索引)

- 保留:查询语言(AND/OR/NOT、ext:、path:、size:、dm:…)、增量收窄缓存、
  相关度 Top-N `partial_sort`、>25 万条并行扫描。
- 列排序改为**对当前结果集按需排序**(≤10 万行,几十 ms,后台线程做)。
- 折叠比较去分配:直接按 `(wchar_t*, len)` 数字感知比较,不构造 wstring。

### 3.4 索引范围与新鲜度策略

| 模式 | 范围 | 新鲜度 | 启动行为 |
| --- | --- | --- | --- |
| 默认(非管理员) | 用户目录 + 自选文件夹 | RDCW 实时 | 映射基座即用;后台低优先级校验重扫(THREAD_MODE_BACKGROUND),仅当基座 >24h |
| 全盘 walk(显式开启) | 全部固定盘 | RDCW 尽力 | 同上,重扫代价用户已知情 |
| MFT(管理员/未来服务) | 全部固定盘 | USN 实时 | 映射基座 + USN 回放进增量,无重扫 |

- 重扫永远在**后台优先级**线程做,产出新基座文件后原子替换,
  期间旧基座照常服务(现 build_/live_ 双份堆内存的问题随 mmap 自然消失)。
- 远期(plan.md 既定方向):独立 `Pulse.Index` 提权服务负责 MFT+USN,
  索引文件本身就是共享格式,UI 进程只映射不构建——本重构是它的直接铺垫。

### 3.5 预期效果对账

| 指标 | 现状 | 重构后 |
| --- | --- | --- |
| 启动到可交互 | 秒~数十秒(锁冻结) | ≤200ms(映射 <10ms,无解析) |
| 启动后磁盘扫描 | 每次全盘重扫 | 基座新鲜则零扫描 |
| 私有工作集(空闲) | ≈220MB | ≈40-60MB(UI 基线;基座是 file-backed) |
| 私有工作集(搜索后) | ≈220MB | +增量与结果集,≤100MB |
| 首次冷搜索 | 快(已在堆上) | +60ms 换页(一次性),之后相同 |
| 索引文件 | 166MB | 162MB(全盘)/ 22MB(默认范围) |
| 保存 | 数秒 + 166MB 快照拷贝 | <1s 整块写,原子替换 |

---

## 4. 实施计划

### R0 止血(半天,不动格式,先让用户能用)

1. `status_` 挪到独立小锁 + `Count()` 原子化 → UI 立即不再冻结;
2. 启动路径跳过 `RebuildSortIndexesLocked`(排序索引临时惰性化);
3. `WalkTree` 每目录批量插入(锁次数 ÷10)。

### R1 重构核心(2~3 天)

1. v6 文件格式 + 整块读写 + 原子替换;基座 mmap 只读化;
2. 删除 by_name_/by_size_/by_mtime_ 与其重建逻辑;列排序改按需;
3. 增量层(append + tombstone + 覆盖表)+ 小锁;RDCW/USN 全部写增量;
4. 后台合并(阈值触发);搜索移出 UI 线程;
5. `(parent, name-hash) → idx` 查找表,替换 O(N) FindChild;
6. 自测:`--selftest` 全绿 + 新增"UI 锁等待 ≤16ms""映射启动 ≤200ms"断言。

### R2 范围与产品(1 天 + 需拍板)

1. 默认索引范围收窄为用户目录 + 自选文件夹(设置页可开全盘);
2. 后台重扫降优先级 + 进度通知限频(≤2 次/秒);
3. 状态栏文案区分:「实时(USN)/实时(监听)/后台重建中」。

### 依赖与顺序

R0 可立即合;R1 内部 1→2→3→4 有依赖,5/6 可并行;R2 依赖 R1 的范围配置落点。
v4/v5 旧缓存直接废弃(检测到旧版本删文件重建一次,代码不背兼容包袱)。

## 5. 风险

- mmap 基座重写/替换窗口:替换时旧映射仍被搜索引用 → 引用计数 + 延迟 unmap,
  R1-4 一并实现,自测覆盖;
- u32 mtime(秒精度,2106 年前有效):`dm:` 与列显示走换算,精度足够;
- 默认范围收窄改变行为:首次运行提示 + 设置项(R2 拍板项);
- 32 位进程无法映射大文件——Pulse 仅 x64,无此问题。

---

## 6. 实施进度(2026-08-16)

已落地,对应 `src/index/index_engine.h` / `index_engine.cpp`。旧 v4/v5 缓存启动时删除并重建。

| 项 | 状态 |
| --- | --- |
| R0-1 Status 独立小锁,Count 原子,UI 不再抢索引大锁 | 完成 |
| R0-2 删除 by_name_/by_size_/by_mtime_,列排序对结果集按需排 | 完成 |
| R0-3 WalkTree 按目录攒齐再插入,重建不持 live 锁 | 完成 |
| R1-1 索引文件 v6 + mmap 只读基座 + `.tmp` 原子替换 | 完成 |
| R1-2 全局排序索引删除(见上) | 完成 |
| R1-3 增量层:heap delta + tombstone + Patch 覆盖表 | 完成 |
| R1-4 后台合并(delta>10 万或 5min);搜索 shared_lock,不阻塞 Status | 完成 |
| R1-5 `(parent, name-hash) → idx` 替换 O(N) FindChild | 完成 |
| R1-6 `--selftest` 索引用例保持 | 完成 |
| R2-1 非管理员默认只索引用户目录 | 完成(全盘开关尚未做设置页) |
| R2-2 缓存 <24h 跳过 walk 重扫;worker `THREAD_MODE_BACKGROUND` | 完成 |
| R2-3 状态「USN 实时 / 监听」;进度通知 ≤2 次/秒 | 完成 |
| 命令面板 Search 仍在 UI 线程 | **完成**：搜索在 `Pulse.Index.exe`，UI 只走管道 + `WM_INDEX_SEARCH` |
| 独立索引进程 + 可选提权服务 | **完成**：`Pulse.Index.exe`（会话助手 / `--install` 装 `PulseIndex` 服务） |

`pulse.exe` 不再调用 `Engine::Start`。未装服务时由 UI 拉起同目录的 `Pulse.Index.exe`（asInvoker，默认只索引用户目录）；命令面板「启用全盘索引（安装服务）」会 UAC 一次，之后以 SYSTEM 做 MFT+USN 全盘索引，登录即驻留。

---

## 7. 右键菜单：融合 Explorer 动词，弹出不许变慢（2026-08-17）

> 需求：按文件类型接入资源管理器右键项（DWG 打开方式、合并 PDF、「新建 ▸」等）。
> Explorer 的病根就是同步加载全部 Shell 扩展（plan.md §1），任何一个扩展卡住整窗卡住。
> 本节结论：**第三方 `IContextMenu` 全部进 `pulse_shell.exe`，UI 首帧永不等 Shell**。

### 7.0 目标（硬指标）

| 目标 | 指标 | 说明 |
| --- | --- | --- |
| 弹出快 | 内置菜单首帧 **≤50ms**（现状预算，不因融合回归） | 首帧只画内置项 + 额外区骨架 |
| 额外项快 | 同扩展名第二次右键，额外项就绪 **≤150ms** | 扩展 DLL 已加载，QueryContextMenu 很快 |
| 冷路径可控 | 首次冷查询（首次加载扩展 DLL）≤1s 出现；**>2s 放弃本轮** | 超时杀 pulse_shell 重启，内置项不受影响 |
| 挂死免疫 | 任一扩展挂死：弹出、内置动词、后续右键全部正常 | Shell 进程无状态，死了自动重启 + 重试一次 |

### 7.1 菜单形态

- 第一行仍是「打开」；剪切/复制/删除/重命名收成**一行四个图标**（复用标签色点条的 `quick_swatches` 命中逻辑，`FluentMenuSwatch` 加 `glyph`）。
- 图标条下方是 Windows 分组：先注册表静态动词（打印/编辑/打开方式应用），后 COM 动态项；按动词过滤与内置重复的 `open/cut/copy/paste/delete/rename/properties` 等。
- **融合区出厂默认丢掉 Windows/分享糟粕**（壁纸、旋转、创建快捷方式、发送到、IM 分享、COM「用 X 打开」重复项）；软件飞出（Bandizip 等）和类型动词（合并 PDF / CAD）保留。有压缩飞出时，顶层「压缩为 zip / 新建压缩包」折进飞出。顺序：飞出 → 类型动词 → 打开方式（静态 MRU 最多 2 +「打开方式…」）→ 打印。Explorer 区默认最多 **12** 行（飞出算一行）；`AppendShellSection` 另有 48 行硬顶。
- 规则写在 `%LOCALAPPDATA%\Pulse\context_menu.json`（分类开关 + 逐项覆盖 + 见过的项）。标题栏齿轮打开窗口内「设置」标签（左侧分类：通用 / 本地右键菜单）；命令面板「设置」和融合区底「管理右键项…」进同一标签。改开关立即保存，**下次右键生效**（菜单开着不热替换）。Host 仍回传全量，过滤在 UI 进程，所以关掉的「发送到」可以再打开。
- 右键按下即预取 COM。**按扩展名缓存上次完整 COM 布局**：第二次起鼠标一松就是全菜单。缓存未命中（该扩展第一次）则等到 `RSP_CTX_ITEMS`（松开后最多 2s）再弹出。弹出后若 live 结果刚到，用同文案换上本会话的命令 id（行数通常不变，没有「先默认后撑开」）。
- **Pulse 自己绝不引入分组子菜单**；软件自带的子菜单（发送到、Bandizip、7-Zip）**保留一级层级**：主菜单只占一行（右侧 › 角标），悬停/点击/→ 打开一级飞出（`FluentMenu` 自绘第二个分层窗口，绝不再嵌套；每组最多 16 项防爆）。拍平成「父 › 子」会把菜单撑得过长，已否决。
- 空白处右键同样走文件夹 `IContextMenu`，「新建 Word/CAD」一类由此获得。

### 7.2 为什么不会慢（架构）

```
右键按下(WM_RBUTTONDOWN)
  ├─ 注册表静态动词: 按扩展名内存缓存
  ├─ 立即发 REQ_CTX_QUERY ──► pulse_shell 每会话独立 STA 线程
  └─ WM_RBUTTONUP:
        有该扩展的 COM 布局缓存 → 立刻弹出完整菜单
        没有缓存 → 等到 RSP_CTX_ITEMS（松开后 ≤2s）再弹出
        live 结果到了：按文案换上本会话 id（行一般不变）
```

三个手段：

1. **按扩展名缓存完整 COM 布局**：第一次右键某种类型会等到 COM（按下已预取，松开后最多再 2s），之后同扩展立刻出全菜单。命令 id 绑定当前 `IContextMenu` 会话，所以 live 一到就按文案换 id，点击走本会话 `InvokeCommand`。
2. **预取**：按下即发查询，松开时 live 常已在队列里（先 Peek，`MsgWait` 对已入队消息不会立刻返回）。
3. **会话即线程**：Query→Invoke→Close 之间 `IContextMenu`/`HMENU` 活在 pulse_shell 的**每会话一条 STA 线程**上；某扩展挂死只废这条线程，文件操作与下次右键不受影响；客户端超时只放弃本轮，不杀进程（避免拖累进行中的复制）。UI 只见 `{id, text, enabled}`。协议新增 `REQ_CTX_QUERY / RSP_CTX_ITEMS / REQ_CTX_INVOKE / REQ_CTX_CLOSE`（protocol.h），命令号 `CmdShellBase = 8000`。

### 7.3 实施要点

1. `pulse_shell`：每会话一条 STA 线程（Query/Invoke/Close 同线程，COM 亲和）；`SHCreateItemFromParsingName`（多选走 `IDataObject`）→ `QueryContextMenu` 填隐藏 `HMENU` → 遍历顶层 + 子菜单按「头行 + 子行」层级回传（`CTX_ITEM_HAS_CHILDREN`/`CTX_ITEM_CHILD`，仅一层）；`GetCommandString` 必须包 SEH（动态子菜单 id 会让第三方处理器越界，Explorer 同款防护）；`InvokeCommand` 的 hwnd 用 Pulse 主窗。
2. IPC/Ops：`ShellClient` 加 4 个消息；`OpsManager` 加异步 `QueryShellMenu`/`InvokeShellMenu`/`CloseShellMenu` 和通用 `ExecuteVerb(path, verb)`，回调 PostMessage 到 UI，不在 reader 线程碰菜单。
3. 注册表静态动词：新模块 `src/app/shell_verbs.*`，progid 动词（`MUIVerb`/默认值/资源串）+ OpenWithList 应用名（版本资源 FileDescription），按扩展名内存缓存。
4. UI：`FluentMenu` 支持模态循环内替换 items 并重新 `Layout`（沿用命令面板 `RequestFilterRefresh` 的安全路径）；`BuildItemMenu` 改图标条。
5. 自测：更新 selftest_1b2 中「item menu 12 项」断言 → 图标条 4 command + 动词过滤/拍平用例（假数据，不打真实第三方扩展）。

### 7.4 风险

- 扩展在 Query 阶段就挂：只废该会话线程，客户端 2s 放弃本轮；文件操作与下次右键不受影响。
- Invoke 弹对话框置父错误：hwnd 传 Pulse 主窗，验证 PDF 合并类带 UI 的动词。
- 菜单已关闭才收到结果：直接 `REQ_CTX_CLOSE`，结果丢弃（会话号防串台）。
- 菜单过长：软件子菜单收进一级飞出（主菜单每组只占一行）；出厂默认丢掉分享/系统噪音并把 Explorer 区封顶 12 行（硬顶 48）；设置标签「本地右键菜单」可逐项改回来。

## 8. 设置标签（标题栏齿轮，2026-08-17）

设置不再弹独立窗口：主题切换按钮左侧齿轮打开一条浏览器式「设置」标签，隐藏文件管理工具栏/侧栏/列表，保留标题栏与状态栏。左侧导航先做两页：

- **通用**：开机自启（HKCU Run `Pulse`）、关闭后继续运行（关到托盘，复制等任务不中断）。写在 `%LOCALAPPDATA%\Pulse\app.json`。
- **本地右键菜单**：上一节的 Explorer 融合区开关。

其它分类以后按同样导航加页。

