# Pulse UI 规范

配套文档：架构与技术栈见 [`plan.md`](./plan.md)。本文只回答一件事：**Pulse 的界面长什么样、每个控件怎么画、动效怎么走**，并且在美观和性能上双验收——两者是同一份规格，不允许「先快后美」或「先美后快」。

参考库：`C:\Users\SS\Desktop\QFluentKit-main`（Qt 的 Fluent 组件库，简称 QFluentKit）。

---

## 1. QFluentKit 怎么用：抄规格，不抄实现

QFluentKit 的价值是它已经把 WinUI 的 Fluent 视觉规格翻译成了具体数字（QSS 里的颜色、圆角、内边距）和具体行为（动效时长、缓动、状态机）。这些是**平台无关的设计事实**，直接搬进 Pulse 的 Direct2D 自绘层。

| 从 QFluentKit 拿走 | 不拿 |
| --- | --- |
| 设计 token：深浅色的透明度阶、圆角、内边距、行高（见 §3） | Qt/QWidget/QSS 本身——Pulse 主进程零 Qt 依赖 |
| 控件清单和状态机：hover/press/checked/disabled 的层级关系 | 每行一个 widget 的实现方式（`QListView::item` 模型撑不住 10 万行） |
| 动效参数：150–250ms、OutQuad/OutCubic（见 §6） | `QPropertyAnimation` 每帧打 CPU 布局的做法——Pulse 动效走 DirectComposition |
| 主题架构：`AUTO/LIGHT/DARK` + 主题色派生 6 阶变体（`ThemeColor::DARK_1..LIGHT_3`） | QSS 运行时换肤机制——Pulse 用编译期 token 表 + 运行时索引 |
| `FluentSystemIcons-Regular/Filled.ttf`（MIT）作为 Win10 图标退路 | 上千张 `_black/_white.svg` 成对位图——Pulse 用图标字体按 token 着色，一份资源双主题 |
| 交互细节：关闭按钮 hover `#c42b1c`、输入框聚焦底边变强调色、覆盖式滚动条 | `LiquidGlassRenderer` 这类 shader 玩具——不进日常工具 |

QFluentKit 里对 Pulse 最有含金量的三个参考实现（读逻辑，重写为 D2D）：

- `src/Private/ScrollBarPrivate.cpp`：覆盖式滚动条的展开/收起状态机与平滑滚动参数。
- `src/QFluent/Menu/RoundMenu.cpp` + `MenuAnimation.cpp`：菜单弹出动画的四种模式与子菜单定位。
- `src/QFluent/FluentTitleBar.cpp`：无边框窗口标题栏的按钮布局、active/inactive 状态、最大化时圆角处理。

---

## 2. 双验收原则

每个 UI 元素同时有两行验收，缺一即打回：

| | 美观验收 | 性能验收 |
| --- | --- | --- |
| 列表 | 与 Win11 Explorer 截图并排，精致度不落下风 | 10 万行 60/120Hz 滚动，无尖刺 |
| 菜单 | 圆角 + 阴影 + 图标列 + 120ms 淡入 | 首帧 ≤ 50ms，动画不阻塞输入 |
| 主题切换 | 深浅色全部控件同帧切换，无闪白 | ≤ 100ms 完成，不重建 GPU 资源以外的东西 |
| 滚动条 | 覆盖式，悬停加宽，Fluent 圆角 | 命中测试 O(1)，不参与行布局 |

---

## 3. 设计 token

Pulse 的 token 表是一个 C++ 常量结构（`ui/FluentTokens.h`），深浅色各一份实例，运行时只换指针。以下数值以 QFluentKit QSS 与 WinUI 规格互相校准得出。

### 3.1 颜色（深色 / 浅色）

文本与填充一律用「白/黑 + 透明度」的叠加层模型，保证在 Mica 上正确混合：

| Token | 深色 | 浅色 | 用途 |
| --- | --- | --- | --- |
| `text.primary` | `#FFFFFF` | `#1A1A1A` | 文件名、正文 |
| `text.secondary` | `#FFFFFF` 72% | `#000000` 61% | 列头、次要信息（QFluentKit：`rgba(236,236,236,0.72)` 非活动标题） |
| `text.disabled` | `#FFFFFF` 40% | `#000000` 36% | 禁用项（QFluentKit menu disabled 0.4） |
| `fill.hover` | `#FFFFFF` 8% | `#000000` 5% | 行/菜单项/按钮 hover（QFluentKit 0.08） |
| `fill.pressed` | `#FFFFFF` 6% | `#000000` 3% | 按下态，比 hover 更淡（QFluentKit 0.06——按下反而变浅是 Fluent 特征） |
| `fill.selected` | 强调色 15% | 强调色 12% | 列表选中底 |
| `fill.input` | `#FFFFFF` 6% | `#000000` 4% | 输入框底（QFluentKit 0.0605） |
| `fill.input.hover` | `#FFFFFF` 8.4% | `#000000` 6% | 输入框 hover（QFluentKit 0.0837） |
| `stroke.card` | `#FFFFFF` 10% | `#000000` 8% | 弹层/卡片描边（QFluentKit menu border 0.1） |
| `stroke.divider` | `#FFFFFF` 8% | `#000000` 6% | 分隔线、列头横线 |
| `stroke.input.bottom` | `#FFFFFF` 54% | `#000000` 45% | 输入框底边（QFluentKit 0.5442，聚焦时换强调色） |
| `surface.flyout` | `#2B2B2B` | `#FBFBFB` | 菜单/浮层实底（QFluentKit `rgb(43,43,43)`） |
| `surface.pane.inactive` | 工具栏文字降到 72% | 同左 | 非焦点栏降对比 |
| `accent` | 系统强调色 | 系统强调色 | 选中、焦点、进度 |
| `accent.text` | 按亮度自动黑/白 | 同左 | 强调底上的文字 |
| `danger` | `#C42B1C` | `#C42B1C` | 关闭按钮 hover、删除确认 |

强调色派生：沿用 QFluentKit `Theme::themeColor(type)` 的思路，从系统强调色按 HSV 派生 `DARK_1..3 / LIGHT_1..3` 六阶，hover/pressed 各取一阶，不手拍六个色值。

### 3.2 几何

| Token | 值 | 出处/说明 |
| --- | --- | --- |
| `radius.window` | 8px | Win11 窗口；QFluentKit 关闭按钮右上角 8px 同步 |
| `radius.flyout` | 8px | 菜单、tooltip、对话框（QFluentKit 9px，取 WinUI 标准 8） |
| `radius.control` | 5px | 按钮、输入框、菜单项、列表选中条（QFluentKit 5px） |
| `spacing.unit` | 4px | 一切间距是 4 的倍数 |
| `row.list` | 28px 默认 / 24px 紧凑 / 36px 触屏 | QFluentKit 35px 偏松，文件管理器取更紧 |
| `row.header` | 32px | QFluentKit 33px 取整 |
| `row.menu` | 32px | 菜单项，含左侧 20px 图标列 |
| `padding.cell` | 左右 12px | QFluentKit item 11px 取 4 的倍数 |
| `titlebar.height` | 56px（含标签）| 两行标签名要有上下留白；标签高度 = 栏高 − 8 |
| `toolbar.height` | 40px | 地址栏 + 操作按钮行 |
| `statusbar.height` | 24px | 每栏底部 |
| `scrollbar` | 静默 2px / 悬停 6px | 覆盖式，QFluentKit ScrollBar 行为 |
| `focusring` | 2px 强调色，外扩 1px | 键盘焦点可见性 |

### 3.3 字体

| 用途 | 字体 | 字号 |
| --- | --- | --- |
| 正文/文件名 | Segoe UI Variable Text，缺失则 Segoe UI | 13px |
| 列头/次要 | 同上 | 12px |
| 地址栏/输入 | 同上 | 13px |
| 面板标题 | Segoe UI Variable Display SemiBold | 14px |
| 控件图标 | **Segoe Fluent Icons**（Win11 内置）；Win10 无此字体时回退 QFluentKit 附带的 `FluentSystemIcons-Regular.ttf` | 16px 网格 |
| 等宽（大小/日期列可选） | 数字用 tabular figures 特性，不换字体 | — |

中文回退链：`Segoe UI Variable → Microsoft YaHei UI`，由 DirectWrite font fallback 定义一次，全局共用。

---

## 4. 控件清单

Pulse 需要的控件远少于 QFluentKit 的全家桶。每个控件 = 一个纯绘制函数 + 一个状态结构，没有 per-item 对象树。

| Pulse 控件 | QFluentKit 参考 | D2D 实现要点 |
| --- | --- | --- |
| 窗口 + 标题栏 | `FluentWidget` / `FluentTitleBar` / `TitleBarButton` | DWM Mica + 自绘非客户区；关闭 hover `danger`；inactive 时标题文字 72% |
| 标签条 | `TabBar` | 标签内嵌标题栏行；活动标签实底 + 上圆角；拖动重排走 DComp 位移 |
| 地址栏（面包屑/编辑双态） | `BreadcrumbBar` + `LineEdit` | 面包屑段 hover `fill.hover`；点击空白转编辑态；聚焦底边 2px 强调色（QFluentKit LineEdit focus 特征） |
| 文件列表（details/图标/紧凑） | `TableView` / `ListView` 仅取视觉 | **自有虚拟列表**；见 §5 |
| 列头 | `QHeaderView` 视觉 | 排序箭头用图标字体；拖宽热区 5px；右键列选择菜单 |
| 树（侧栏导航） | `TreeView` | 同一虚拟列表引擎，缩进 + chevron；固定项/盘符/云节点 |
| 右键菜单/下拉 | `RoundMenu` + `MenuAnimation` | 独立分层窗口（`WS_EX_LAYERED`）；圆角 8 + `stroke.card` + 阴影；见 §6 动效 |
| 按钮（普通/主色/图标） | `PushButton` / `ToolButton` | 三态填充层；主色钮用 `accent`，文字 `accent.text` |
| 输入框 | `LineEdit` | 过滤框、重命名框共用；内嵌清除钮 |
| 复选/开关/单选 | `CheckBox` / `SwitchButton` / `RadioButton` | 仅设置页与冲突对话框用 |
| 进度（条/环） | `ProgressBar` / `ProgressRing` | 复制任务卡片、标签页忙碌指示；不确定态用环 |
| 信息条 | `InfoBar` | 栏顶部内联提示（网络盘超时、权限不足），不弹模态 |
| Tooltip | `ToolTip` | 延迟 400ms，跟随 token；截断文件名悬停显示全名 |
| 对话框 | `MessageBoxBase` / `MessageDialog` | 冲突/删除确认；模态只罩本窗口 |
| 滚动条 | `ScrollBar`（覆盖式） | 见 §5.4 |
| 状态栏 | — | 项数/选中数/容量/后台任务摘要；无参考直接按 token 画 |
| 命令面板 | `RoundMenu` 弹层视觉 + `LineEdit` | `Ctrl+K` 居中浮层；输入行 + 结果虚拟列表；命令/跳转/搜索统一入口（§7.2） |
| 暂存托盘卡片 | `CardWidget` / `InfoBar` 视觉 | 侧栏底部常驻；批次卡片 + 逐项行 + 释放按钮；拖放目标（§7.4） |
| 标签色点 | `InfoBadge` 视觉 | 8px 实心圆 + 1px 背景描边；行内最多叠 3 个；侧栏标签项带计数（§7.5） |
| 连接状态点 | `InfoBadge` | 网络位置项前 6px 圆点：在线/慢/离线三色（§7.7） |
| 任务进度浮层 | `ProgressBar` + `ToastToolTip` 视觉 | 状态栏点击展开；每任务一行：名称/进度/暂停/取消 |
| 设置页 | `SettingCard` / `SettingCardGroup` | 阶段 4+；卡片分组风格照搬 |

不做的 QFluentKit 控件：日历/时间选择、分页、Tour、Tag 输入、LiquidGlass 系、ProfileCard——文件管理器用不上，不进代码库。

---

## 5. 文件列表规格（核心控件，单独立规）

### 5.1 结构

```text
Pane
 ├─ 列头行（32px，置顶不滚动）
 ├─ 视口（虚拟行，只画可见区 ±1 屏）
 │    行 = [选择框?] [图标 16px] [名称(弹性)] [修改时间] [类型] [大小]
 └─ 覆盖式滚动条（不占布局宽度）
```

### 5.2 行状态（叠加层从下到上）

1. 底：透明（偶数行可选斑马纹 `#FFFFFF` 2%，默认关）
2. hover：`fill.hover`，圆角 5px，左右缩进 4px
3. 选中：`fill.selected` + 左侧 3px 强调色条
4. 选中+hover：selected 上再叠 hover
5. 键盘焦点：`focusring` 圆角框（可与选中并存）
6. 剪切待移动：整行 55% 不透明度
7. 拖放目标（文件夹行）：强调色 2px 描边

### 5.3 内容规则

- 名称列弹性伸缩，中间省略（`...` 用 DirectWrite trimming，保留扩展名）。
- 大小/日期右对齐，tabular figures 对齐数位。
- 重命名 = 行内输入框覆盖名称区域，其余列原样；`F2` 进入，选中主名不选扩展名。
- 图标来自扩展名 atlas；atlas 未命中先画通用文件图标，Shell 进程回填后仅重绘该行。
- 云占位徽章画在图标右下角 8px，不触发注水。

### 5.4 滚动

- 滚轮：每格 3 行，120ms OutSine 平滑（QFluentKit `ScrollBar.cpp` 300ms 对文件列表太肉，减半再减）。
- 拖滚动条 / `PgUp/PgDn`：直接跳，无动画。
- 滚动条静默 2px 半透明；指针进入 8px 热区后 150ms 展开到 6px 圆角滑块；离开 800ms 后收起。
- 惯性滚动只改视口偏移，绝不触发同步 I/O；预取窗口见 `plan.md` §6。

### 5.5 性能红线

- 一帧内：可见行 × 每行 ≤ 6 个 D2D 绘制调用（底、图标、3–4 段文本）。
- 文本布局（`IDWriteTextLayout`）按行缓存，滚动复用，宽度变化才重建。
- hover 变化只标脏单行；选中变化标脏受影响行集合。
- 图标 atlas 常驻 GPU；每帧零上传（回填走独立提交点）。

---

## 6. 动效表

全部动效走 DirectComposition/合成器，CPU 每帧零参与；**用户任何输入立即取消当前动画并跳到终值**。时长以 QFluentKit 实测参数为基准微调：

| 场景 | 参数 | QFluentKit 出处 |
| --- | --- | --- |
| 菜单弹出 | 淡入 120ms + 下滑 8px，OutQuad | `MenuAnimation.cpp` FADE_IN_DROP_DOWN 150ms，工具场景收紧 |
| 菜单收起 | 淡出 80ms | 同上 FAST_DISMISS |
| Tooltip | 淡入 100ms，无位移 | `ToolTip` |
| 标签切换 | 内容交叉淡化 100ms | `StackedWidget` |
| 标签拖动 | 跟手 0ms；松手归位 150ms OutCubic | `TabBar` |
| 栏分割条拖动 | 跟手 0ms，无动画 | — |
| 预览面板开合 | 宽度 200ms OutCubic | `Drawer` 250ms 收紧 |
| 滚动条展开/收起 | 150ms / 250ms OutCubic | `ScrollBarPrivate.cpp` 250ms |
| 平滑滚轮 | 120ms OutSine | `ScrollBar.cpp` 300ms OutSine 收紧 |
| 主题切换 | 全局同帧换 token 指针；可选 250ms 圆形 reveal（参考 `ThemeRevealOverlay`），默认关 | `Theme::setThemeAnimated` |
| 进度条不确定态 | 1.5s 循环滑块，合成器动画 | `ProgressBar` |
| 列表行 | **无进出场动画**——大目录任何 per-row 动画都是灾难 | 有意偏离 QFluentKit |

---

## 7. 窗口与布局

设计参考：`design/pulse-ui-concept.png`（概念样机）。布局哲学不是复刻 Explorer，而是围绕两个高频场景优化：**在一个项目的多个子文件夹之间搬文件**、**频繁访问内网共享**。核心手段：暂存托盘代替「开两个窗口对拷」、工作区代替「每次从盘符点进去」、能拖则拖、能一步则一步。

### 7.1 总体结构

```text
┌───────────────────────────────────────────────────────────────┐
│ ⌂ [标签A][标签B][+]      [ 命令面板 / 搜索  Ctrl+K ]  ☾ ─ □ ×  │ 44px 标题栏
├───────────────────────────────────────────────────────────────┤
│ ← → ↑ ⟳ │ 面包屑地址栏 │ [新建▾] ✂ ⧉ 📋 ✎ 🗑 │ 分栏 视图 详情 │ 40px 工具栏
├──────────────┬────────────────────────────────────────────────┤
│ 工作区        │  栏（1..n，默认 1 栏）                          │
│  ▸ 项目A ●git │   ┌ 栏头：当前位置摘要 ── 过滤框(名称/标签) ┐   │
│  ▸ 项目B      │   │ 列头：名称  修改日期  标签  大小        │   │
│ 快速访问      │   │ 文件列表（虚拟行）                       │   │
│  ★ 星标  ⏱最近│   │                                        │   │
│ 磁盘（容量条） │   │                                        │   │
│ 标签          │   └────────────────────────────────────────┘   │
│  ●紧急 ●审阅  │                                                │
│ 网络位置      │                                                │
│  ⚡NAS ⚡内网02│                                                │
├──────────────┤                                                │
│ 暂存托盘      │                                                │
│  [批次1: 3项] │                                                │
├──────────────┴────────────────────────────────────────────────┤
│ 状态栏：选中摘要 │ 项数/容量 │ 后台任务 │ 编码/模式          24px │
└───────────────────────────────────────────────────────────────┘
```

- **默认单栏 + 侧栏 + 托盘**。托盘承担了传统双栏「对拷」的职责，新用户不需要先理解多栏；双栏/四宫格仍一键切换（工具栏「分栏」+ 预设菜单），多栏规则见 `plan.md` §7.1。
- 侧栏 200–280px 记忆宽度，可折叠到 48px 纯图标（托盘折叠成一个带角标的图标，拖放悬停时自动展开）。
- 窗口最小 640×420：单栏 + 侧栏折叠 + 托盘图标态仍完整可用。
- Win10 降级：无 Mica → `surface.flyout` 实底；无 Segoe Fluent Icons → FluentSystemIcons ttf；布局不变。

### 7.2 顶部两行

**标题栏（44px）**：应用钮 + 标签组 + 命令面板入口 + 主题切换 + 窗控。标签属于窗口级（多栏共享标签组时切标签 = 切整套布局快照），比每栏一条标签更省认知——栏内导航靠历史（`←/→`）和面包屑。

**工具栏（40px）**从左到右三簇：

| 簇 | 内容 | 规则 |
| --- | --- | --- |
| 导航 | `← → ↑ ⟳` + 面包屑 | 面包屑每段可点、可放下拖拽（拖文件到某段 = 移到那级）；末段下拉兄弟文件夹；点空白转编辑态 |
| 操作 | `新建▾ 剪切 复制 粘贴 重命名 删除` | 图标钮 + tooltip；随选中态启用/禁用；「新建▾」下拉：文件夹 / 文本文件 / 粘贴为文件 |
| 视图 | 分栏预设、列表/网格/详情、右侧详情面板开关 | 全部是状态可见的 toggle |

**命令面板（`Ctrl+K`）** 是所有「想不起快捷键」操作的兜底：输入即模糊匹配 ①命令（"新建文件夹"、"复制路径"）②当前工作区内子文件夹跳转 ③全盘搜索（`/` 前缀）④最近路径。一个入口，代替记忆十个菜单位置。

### 7.3 侧栏：五个分组 + 托盘

自上而下：**工作区、快速访问、磁盘、标签、网络位置**，各组可折叠、顺序可拖调。所有侧栏项都是**拖放目标**（见 §7.8）。

| 分组 | 内容 | 行为 |
| --- | --- | --- |
| 工作区 | 用户钉住的项目根目录 | 见 §7.6 |
| 快速访问 | 星标常用文件 + 最近使用 | 星标是路径索引（`places.json` 的 `starred`），列表显示真实文件/文件夹，不复制、不放 `.lnk`；最近自动记录、可清除 |
| 磁盘 | 本地卷 + 容量条 | 容量条低于 10% 变 `danger` 色；点击进根 |
| 标签 | 色彩标签列表 + 计数 | 见 §7.5 |
| 网络位置 | 钉住的 UNC 共享 + 连接状态点 | 见 §7.7 |

### 7.4 暂存托盘（核心差异化，阶段 1 就做）

解决「跨文件夹搬运要开两个窗口」的问题：先收集，后释放，中间随便导航。

**进入托盘（三种方式）**：

1. `Ctrl+C` / `Ctrl+X` 自动进入（默认开，设置可关）——剪贴板照常工作，托盘只是把剪贴板历史可视化；
2. 拖文件到托盘区域（或折叠态的托盘图标上）；
3. 右键「放入暂存」。

**托盘结构**：按**批次**组织。一次 `Ctrl+C` 选中的 N 个文件 = 一张批次卡片（显示前两个文件名 + 「等 N 项」+ 总大小 + 复制/移动意图角标）。多次收集 = 多张卡片叠放，互不干扰。

**释放（三种方式）**：

1. 到达目标目录后点批次卡片上的「释放到当前文件夹」——一步完成；
2. 把批次卡片（或卡片里的单项）拖出到任意栏 / 面包屑段 / 侧栏文件夹；
3. `Ctrl+V` 释放最新批次（保持剪贴板肌肉记忆完全不变）。

**规则**：

- 托盘存**路径引用**，不复制数据；源文件被外部删除时卡片项标灰并提示。
- 剪切批次释放后自动移除；复制批次默认也移除（设置可改为保留，用于一处收集多处分发）。
- 托盘跨标签、跨栏、跨窗口共享（单实例内全局），会话恢复后仍在。
- 批次卡片可整卡关闭（×）、可展开逐项移除。
- 容量角标常显（概念图的「2 项」），托盘为空时显示一行提示文案 + 虚线拖放框。

### 7.5 色彩标签（访达模式）

- **7 种颜色 + 可改名**（默认：红/橙/黄/绿/青/蓝/紫），名称如「紧急修补」「设计审阅」由用户定义。
- **打标签，能拖则拖**：拖文件到侧栏标签项 = 打标；右键菜单一排色点一击即打；快捷键 `Ctrl+Shift+1…7`。再次操作同色 = 取消。
- **显示**：列表行名称右侧最多叠 3 个 8px 色点（概念图样式）；可选「标签」列显示名称。
- **侧栏标签项** = 保存的查询：点击后当前栏变成「该标签的虚拟目录」（跨文件夹聚合，走索引），计数实时。作用域默认当前工作区，可切全盘。
- **过滤**：栏头过滤框支持 `#红` / `#紧急修补` 语法，与名称过滤可组合。
- **存储**：NTFS 上写文件 ADS（随文件移动/复制自带）；同时进本地库做索引与回退（FAT/网络盘不支持 ADS 时只用库）。两处不一致以文件 ADS 为准。

### 7.6 工作区：为「一个项目多个文件夹」提速

工作区 = 钉住的项目根目录 + 记住的使用状态。目标：进入项目 → 一步回到昨天的工作现场。

- **钉**：把项目根拖到侧栏「工作区」组，或右键「钉为工作区」。
- **进入 = 恢复布局快照**：栏数、各栏路径、排序、过滤、选中标签作用域全部还原。做完「A 项目图纸区 + 成果区」双栏布局后，下次一键回来。
- **项目内跳转 `Ctrl+P`**：模糊匹配项目树内的子文件夹（走索引，毫秒级），输入「结施」直接跳 `…\03_结构\结施图\`——这是「多个文件夹之间操作」的最大提速点。
- **常去子文件夹**：工作区项可展开，自动按访问频率列出前 8 个子文件夹，点击直达；可手动钉住覆盖自动排序。
- **每工作区独立最近文件**、独立标签计数作用域。
- Git 徽章（概念图的分支/状态列）：检测到 `.git` 时可选开启，状态由独立后台进程产出，**永不进列表首帧路径**；归阶段 5。

### 7.7 网络位置：内网共享不再是二等公民

内网慢的根源是同步等待。Pulse 的原则：**网络目录永远先出缓存，后台再刷新**。

- **钉住 UNC**：`\\server\share` 钉到侧栏，凭据存 Windows 凭据管理器，按 share 记忆。
- **连接状态点**：绿色在线 / 黄色慢（探测 RTT 超阈值）/ 灰色离线。后台轻量探测（打开工作区或悬停时才探测，不做常驻轮询打爆网络）。
- **快照持久化**：网络目录的列表快照落盘。再次进入时**立即显示上次快照**（标注「缓存 · 3 分钟前」横幅），后台增量刷新后原位更新。离线时快照只读可浏览，操作按钮禁用。
- **预热**：进入工作区时，后台预取该工作区引用的网络文件夹快照——等你点进去时列表已经在了。
- **断网不冻**：所有网络等待都在 worker（`plan.md` §6.2），该栏显示骨架 + 状态横幅，其余 UI 照常。
- 传输大文件到内网走复制队列，进度在状态栏任务区，可暂停。

### 7.8 拖放规则：能拖则拖

所有可见目标都可放下，修饰键与 Explorer 一致（同盘默认移动、跨盘默认复制、`Ctrl` 强制复制、`Shift` 强制移动、`Alt` 建快捷方式，拖放中显示动作徽章）：

| 拖到 | 效果 |
| --- | --- |
| 另一栏 / 列表里的文件夹行 | 移动/复制 |
| 面包屑任意段 | 移动/复制到那一级 |
| 标签页头 | 悬停 600ms 先切过去（spring-loaded），放下进该目录 |
| 侧栏文件夹 / 工作区 / 磁盘 / 网络位置 | 移动/复制 |
| 侧栏色彩标签 | 打标签（不移动文件） |
| 暂存托盘 | 暂存 |
| 文件夹行悬停 800ms | 自动进入该文件夹继续拖（spring-loaded，`Esc` 退回） |
| 拖出到 Explorer / 其它程序 | 标准 OLE，照常工作 |

### 7.9 一步操作清单（高频操作的步数预算）

| 操作 | Pulse 步数 | Explorer 对照 |
| --- | --- | --- |
| 跨文件夹收集 3 处文件再统一放置 | 3 次 `Ctrl+C` 进托盘 + 1 次释放 | 开两窗对拖 3 轮 |
| 回到项目工作现场 | 点工作区（恢复布局） | 逐个导航 N 步 |
| 跳项目内某子文件夹 | `Ctrl+P` + 两三个字 | 面包屑/树逐级点 |
| 给文件打「紧急」标 | 拖到侧栏红标签 / `Ctrl+Shift+1` | 无此功能 |
| 复制文件完整路径 | `Ctrl+Shift+C` | Shift+右键找菜单项 |
| 在此处开终端 | 右键内置动词 / `Ctrl+K` 打字 | 右键翻菜单 |
| 新建文件夹并进入 | `F7` 输名回车（默认进入） | 新建→双击 |
| 打开内网常用共享 | 点侧栏（先出缓存） | 输 UNC + 等待 |

原则：**任何每天 ≥ 5 次的操作，必须同时有「一次拖放」和「一个快捷键」两条路径**；做不到的功能设计打回。

### 7.10 视觉补充

- 栏头（概念图「工作区 A · 14 个项目 · 已选 1 项」那行）：左侧位置摘要 `text.secondary`，右侧过滤框，高 32px。
- 托盘卡片：`surface.flyout` 底 + `stroke.card` 描边 + 圆角 8px，是侧栏里唯一的「卡片」，视觉权重刻意高于普通侧栏项。
- 标签色点用纯色实心圆，不透明度不随主题变（红就是红），深浅主题下外加 1px 背景色描边保证可见。
- 状态栏右侧固定放后台任务摘要（「复制到 NAS 34%」），点击展开任务列表浮层。

---

## 8. 主题与 DPI

- 主题模式 `AUTO/LIGHT/DARK`（沿 QFluentKit 三态），AUTO 监听系统 `UISettings::ColorValuesChanged` 与注册表 `AppsUseLightTheme`。
- 切换实现：token 表两份常驻，切换 = 换指针 + 全窗口标脏一次 + 通知 DWM 换 backdrop 深浅；禁止逐控件刷样式。
- 高对比模式：检测到 `HighContrast` 时放弃透明度叠层，直接使用系统 `GetSysColor` 色板，禁用 Mica 与全部动效。
- DPI：Per-Monitor V2；brush/文本格式/图标 atlas 按 DPI 建缓存键；96/120/144/192 四档实测验收，1px 描边按物理像素对齐（`0.5px` 偏移规则统一封装）。

---

## 9. 图标策略

| 类别 | 来源 | 渲染 |
| --- | --- | --- |
| 控件图标（箭头、排序、关闭…） | Segoe Fluent Icons 字体，Win10 回退 FluentSystemIcons ttf | DirectWrite 按 `text.*` token 着色，天然双主题 |
| 文件类型图标 | 系统 `SHGetFileInfo`/`IImageList`（经 Shell 进程） | 解码一次进 GPU atlas，键 = 扩展名 + DPI 档 |
| 盘符/特殊文件夹 | 系统 `imageres.dll` 索引 | 同上 |
| 云状态/链接徽章 | Fluent 图标字体叠加 | 图标右下角 8px |

禁止：手绘一套文件图标；每个文件调一次 `SHGetFileInfo`；把 QFluentKit 的 `_black/_white.svg` 成对资源模式搬过来。

---

## 10. 无障碍与输入

- 虚拟列表实现 UIA Provider（`IRawElementProviderSimple` + Table/Selection/ScrollItem pattern），报告真实行数与可见范围——自绘不等于黑箱。
- 全键盘可达：焦点环只在键盘导航时显示（`Tab`/方向键激活，鼠标点击隐藏），行为对齐 WinUI FocusVisual。
- 触屏：行高切 36px 档，滚动条常显 6px；长按 = 右键。
- IME：地址栏/过滤/重命名的输入框走标准 `TSF`，候选窗定位到光标；这是自绘 UI 最容易糊的地方，阶段 1 即验收中文输入。

---

## 11. 实施顺序与验收

token 表和绘制原语先行，控件按依赖排序：

1. **阶段 0**：`FluentTokens.h` 双主题表 + D2D 绘制原语（圆角矩形、文本、焦点环）+ 10 万行虚拟列表样张（深/浅 × 100/150% DPI 四张截图）。
2. **阶段 1**：标题栏 + 标签条 + 地址栏 + 列表 + 右键菜单 + 滚动条 + tooltip + 侧栏（快速访问/磁盘）+ **暂存托盘**（MVP 九件套）。
3. **阶段 2**：命令面板、工作区组、标签组与色点、网络位置组、多栏框、信息条、任务进度浮层。
4. **阶段 3+**：预览面板、对话框、设置页卡片。

每个控件合入前的固定验收：

- [ ] 深/浅/高对比三主题截图
- [ ] 100/125/150/200% DPI 无糊、描边对齐物理像素
- [ ] hover/press/disabled/focus 四态齐全且与 token 表一致
- [ ] 与 Win11 Explorer 同功能区域并排截图，精致度不落下风
- [ ] 帧时间：该控件所在窗口交互中 P99 ≤ 8ms（120Hz 余量）
- [ ] 中文渲染与 IME 输入正常
